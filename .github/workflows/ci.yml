name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      DEVENV_NO_GIT_HOOKS: "1"
    steps:
      - uses: actions/checkout@v4

      - name: Install Nix
        uses: cachix/install-nix-action@v27
        with:
          github_access_token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Magic Nix Cache
        uses: DeterminateSystems/magic-nix-cache-action@v8

      - name: Install devenv
        run: |
          nix profile install --accept-flake-config github:cachix/devenv/latest

      - name: Cache Mix dependencies
        uses: actions/cache@v4
        with:
          path: |
            deps
            _build
          key: ${{ runner.os }}-mix-${{ hashFiles('**/mix.lock') }}
          restore-keys: |
            ${{ runner.os }}-mix-

      - name: Get dependencies
        run: |
          devenv shell mix deps.get

      - name: Compile
        run: |
          devenv shell mix compile --warnings-as-errors

      - name: Check formatting
        run: |
          devenv shell mix format --check-formatted

      - name: Check C++ formatting
        run: |
          devenv shell task cpp:format:check

      - name: Run C++ static analysis
        run: |
          devenv shell task cpp:check

      - name: Run tests
        run: |
          devenv shell mix test

      - name: Run production benchmarks
        run: |
          devenv shell mix test test/production_benchmark_test.exs --include production_benchmark --only production_benchmark

      - name: Run AddressSanitizer tests
        run: |
          devenv shell task test:asan

      - name: Run full checks
        if: github.event_name == 'pull_request'
        run: |
          devenv shell mix check
